# 从零手搓 Harness 之上限：给运行装上两条刹车

## 前言

这是从零开始搭建 Agent Harness（`go-harness`）的第八篇。第三篇把主循环装上的时候，退出条件写了两个：模型不再调工具，或者保险丝烧了。那条保险丝叫 `maxTurns`，默认 100，撞上就报 `ErrRunLimitExceeded` 退出。

跑到今天（第七篇上下文压缩也写完了）回头看，这条保险丝是哑的：它只会数数。模型规规矩矩跑了 99 轮，每轮参数都不一样、每轮都在原地打转，它看不见；反过来，一个真有进展的活只要战线拉长一点，它一刀切下来。它管的是"跑了多少轮"，管不了"这几轮在干什么"。

这一篇加第二条刹车，管形态的：连续若干轮的工具调用与结果一模一样，就判定卡住了，报一个新的错误码（50001），带上已产生的消息退出。两条刹车并存，各报各的码。

完整代码参考 GitHub：[https://github.com/PycMono/go-harness](https://github.com/PycMono/go-harness)，本文涉及的文件（`pi/loop.go`、`pi/loopguard.go`、`pi/agent.go`、`pi/error/errors.go`）都在仓库里可以直接翻。

## 流程图

两条刹车的判定点分别在轮的两头：

```text
for turn := 0; ; turn++ {
    │
    ├─ 轮首 ── ctx 取消了？turn 到上限了？        ← 第一条：数总量（50000）
    │
    ├─ beforeTurn（压缩挂在这里，换历史段）
    │
    ├─ 调一次模型
    │       │
    │       └─ 模型没带工具调用 ──► 运行正常结束
    │
    ├─ 执行这一批工具、结果回填进消息序列
    │
    └─ 轮末 ── afterTurn(TurnReport)              ← 第二条：看形态（50001）
                  │
                  └─ 判据说这是循环 ──► 带消息返回，不再开下一轮
}
```

一句话：轮首那条管"跑了多少轮"，轮末那条管"这几轮在干什么"。

## 代码层级划分

```text
pi/
├── loop.go        改：TurnReport、AfterTurn、WithAfterTurn，run 里两处判定
├── loopguard.go   新增：签名、连续计数、错误文案
└── agent.go       改：Options.LoopGuardTurns、装配钩子、Run 开头重置
```

| 文件 | 职责 | 对外 |
|---|---|---|
| `pi/loop.go` | 轮次边界（`Turn` / `BeforeTurn` / `AfterTurn`）与轮首的上限判定 | `TurnReport` / `AfterTurn` / `WithAfterTurn` / `WithMaxTurns` |
| `pi/loopguard.go` | 内建判据：一轮的签名、连续计数、错误文案 | 不导出 |
| `pi/agent.go` | 阈值旋钮与装配 | `Options.LoopGuardTurns` |

没有新包——判据只有一条，状态只有两个字段，一个文件够了。对外只多三个名字加一个配置字段，检测器本身不导出：它是策略，形状不该对外承诺，将来要换一条判据也不会动到调用方。

判据这一摊子全是纯函数（签名计算、参数规范化、文案截断），没有状态、不碰文件、不调模型，拿两个切片就能算。这是有意的：判据是这一层唯一会判错的地方，能单独算才验得动。

## 代码实战

### 1. 轮首那条：数总量

`pi/loop.go`，先是那个常量：

```go
// defaultMaxTurns 是单次运行内允许的模型调用次数上限。每次模型消息只要带回
// 工具调用就消耗一轮，模型不再调用工具即正常结束；撞到上限说明模型陷入
// 自我循环，直接报错退出而不是无限烧 token。
const defaultMaxTurns = 100
```

然后是循环开头的那两处退出判定：

```go
	for turn := 0; ; turn++ {
		if err := ctx.Err(); err != nil {
			return state.messages(), pierrors.ErrCanceled.Wrap(fmt.Errorf("agent 运行已取消: %w", err))
		}
		if turn >= l.maxTurns {
			return state.messages(), pierrors.ErrRunLimitExceeded.Wrap(
				fmt.Errorf("连续 %d 轮模型调用都要求执行工具，已终止运行", l.maxTurns))
		}
```

三个地方值得看。

一是这个默认值住在 `Loop` 里，不在 `Agent` 里。`defaultMaxTurns` 在 `NewLoop` 里就生效（`pi/loop.go:106`），`WithMaxTurns` 只覆盖它，`<= 0` 就退回默认。所以谁自己拼 `Loop` + `Scheduler`、不走 `Agent`，也白得一个调用次数上限。这一点后面讲两个刹车为什么不合并时还会用到。

二是 `turn >= l.maxTurns` 里那个 `turn` 是从 0 起数的，判定在每次模型调用之前：第 0 轮先跑，跑满 100 轮到第 101 次进来时被拦下。写 `>` 还是 `>=` 差一轮，无所谓，但要知道自己数的是什么。

三是退出的形状：带消息返回。`state.messages()` 跟着错误一起回去，调用方拿到的是前 100 轮的完整对话，出问题的时候能直接看到挂在哪一步，而不是一个孤立的 error。上面那个 ctx 取消的分支是同一个形状。

### 2. 轮末那条：轮次结束的钩子

循环要给上层开口子，得先想清楚开在哪儿。`pi/loop.go` 里原来只有一个 `BeforeTurn`，在每轮调模型之前、只能替换请求的历史段。检测器要的是它的对面：这一轮的工具结果已经写回、下一轮还没开始。

```go
// TurnReport 是一轮的现场：这一轮的助手消息与它带出的工具结果。只有带工具
// 调用的轮才会产生它——模型不再要求调用工具时运行就该结束了，没有"还要不要
// 开下一轮"这个问题要问。
type TurnReport struct {
	// Index 是轮次序号，从 0 起，与 run 里 for 的计数一致。
	Index int
	// Message 是这一轮的助手消息。
	Message *schema.AssistantMessage
	// ToolResults 是这一轮的工具结果消息，下标与 Message.ToolCalls 对齐——
	// 判据按下标把结果配到调用上，所以这个对齐是签名正确性的一部分。
	ToolResults schema.Messages
}

// AfterTurn 在每轮的工具结果写回之后调用，是本包里唯一一个"轮次结束"的开口。
// 返回 nil 表示继续；返回错误则收尾——循环把已产生的消息连同这个错误一起交回，
// 不掐 provider 流、不取消正在跑的工具，只是不再开下一轮。
type AfterTurn func(ctx context.Context, report TurnReport) error

// …（中间是 Loop 结构本身，与钩子无关，略过）

// WithAfterTurn 给循环接一个轮次结束钩子。不设置时行为不变。
func WithAfterTurn(hook AfterTurn) LoopOption {
	return func(loop *Loop) { loop.afterTurn = hook }
}
```

调用点在 `pi/loop.go:270`，工具批执行完、结果回填之后，`for turn` 的下一次迭代之前：

```go
		// 每轮的工具结果写回之后给上层一次机会判定这是不是循环。判定放在这里
		// 而不是每轮开头：只有"还要开下一轮"的那些轮才有这个问题，模型不再要
		// 工具时上面已经返回了。返回错误就是收尾，与上面 maxTurns 同一种形状
		// ——带消息返回，不中断正在跑的东西。
		if l.afterTurn != nil {
			if err = l.afterTurn(ctx, TurnReport{
				Index:       turn,
				Message:     assistant,
				ToolResults: toolResults,
			}); err != nil {
				return state.messages(), err
			}
		}
```

这里有两处取舍要说。

一个是钩子返回 `error`，不是 `bool`。判断"要不要停"用 `bool` 是更直觉的形状，但这一层的运行已经是 `(RunOutput, error)` 的形状了：返回值里白揣着一个能带错误码的位置，只回一个 `true` 就等于把"为什么停"丢掉，调用方拿到的是"停了"，分不出是循环、是超预算、还是别的什么。

另一个是命名对称、能力不对称。`beforeTurn` 返回非 nil 时替换历史段（`pi/loop.go:215`），`afterTurn` 返回错误时终止整次运行。轮次开始处能改的是请求，轮次结束处能改的只有"要不要继续"——这不是没写完，是那个位置本来就只有这一个可用动作。

### 3. 一轮的签名

判据的全部就是这一轮的签名：批内每个调用按原顺序取「工具名 + 规范化参数 + 结果」，用换行拼起来。

```go
// signatureOf 是一轮工具调用与结果的指纹：批内每个调用按原顺序取「工具名 +
// 规范化参数 + 结果」，再用换行拼起来。不做排序——调度器按调用在批内的原顺序
// 分波执行，[read a, write b] 与 [write b, read a] 的结果可能不同，排了序会把
// 它们算成同一轮。
//
// 分隔符用换行：结果文本里本来就有换行，所以严格说两条不同的记录理论上能拼出
// 同一个串（要结果里写出一段像下一条调用前缀的文字，还得连着三轮一样）。这
// 不是安全边界，撞了的后果是多停一次运行，不为它换一个更怪的字符。
func signatureOf(calls schema.ToolCalls, results schema.Messages) string {
	parts := make([]string, 0, len(calls))
	for index, call := range calls {
		part := call.Name + ":" + canonicalArguments(call.Arguments)
		// 结果按下标对齐（ExecuteBatch 的返回与 calls 一一对齐）。长度不一致时
		// 退化成只看调用：判据弱一点，比越界 panic 好——钩子在运行路径上，
		// panic 会把整次运行连同已产生的消息一起丢掉。
		if index < len(results) {
			part += "=>" + resultFingerprint(results[index])
		}
		parts = append(parts, part)
	}

	return strings.Join(parts, "\n")
}
```

不排序这条，理由在调度器里。`ExecuteBatch` 按调用在批内的原顺序分波执行，每一波的结果按下标写回：

```go
// ExecuteBatch 执行一批工具调用，返回与 calls 下标一一对齐的结束事件。
// 并发粒度按「波次」切分：连续若干个可并发的工具合成一波并发执行，不可并发的
// 工具各自独占一波串行执行。是否为可并发由工具定义里的 ParallelSafe 声明，
// 判断见 Registry.ParallelSafe。
// 返回的 error 非 nil 时 results 只是部分有效（取消前已完成的那些调用）。
func (s *Scheduler) ExecuteBatch(ctx context.Context, calls schema.ToolCalls) ([]Event, error) {
	results := make([]Event, len(calls))

	for start := 0; start < len(calls); {
		end := start + 1
		if s.registry.ParallelSafe(calls[start].Name) {
			for end < len(calls) && s.registry.ParallelSafe(calls[end].Name) {
				end++
			}
		}

		if err := s.executeWave(ctx, calls, results, start, end); err != nil {
			return results, err
		}
		start = end
	}

	return results, nil
}
```

顺序在这条链路上携带语义：`[read a, write b]` 与 `[write b, read a]` 的结果可能不同。把顺序抹平，两者会被算成同一轮——那是误报方向，误报的代价是掐掉一次健康的运行。

结果也要进签名，这条是整个判据里最要紧的一处。只看调用的话，轮询类工具必被误杀：模型连着三轮问同一个任务的状态，调用一字不改，第三次刚返回"完成"，运行就被判成循环终止了，模型永远看不到那个结果。加上结果，判据收紧，能停的形态变少——方向上只会减少误报。

```go
// resultFingerprint 把一条工具结果压成可比的一行：各内容块的文本。带上结果比
// 不带严——调用相同、结果也相同才叫"这一轮什么也没变"，于是轮询类工具（连着
// 追问同一个状态，第三次返回"完成"）不会被误停。那是误报方向，本方案一路都在
// 躲它。
//
// 图片块取「媒体类型 + 载荷长度」，不取内容摘要：同类型、同长度的两张图会撞成
// 同一条结果（又是误报方向，但概率低得多）。真出现图片类工具再加摘要，现在
// 不为它引一个 crypto。
//
// 不单独带 IsError：内容一样就是没进展，成败那一栏由内容体现，多一栏不改变
// 判定，反而让"同一段报错、一次算失败一次算成功"变成两轮。
func resultFingerprint(message schema.Message) string {
	blocks := message.Blocks()
	parts := make([]string, 0, len(blocks))
	for _, block := range blocks {
		if block.Image != nil {
			parts = append(parts, fmt.Sprintf("[image %s %d]",
				block.Image.MIMEType, len(block.Image.Data)))
			continue
		}
		parts = append(parts, block.Text)
	}

	return strings.Join(parts, "\n")
}
```

结果用 `Message.Blocks()` 读内容，不断言成 `*schema.ToolResultMessage` 再读 `Content`：两者的值一样，但前者不需要断言，也就没有"断言失败怎么办"这个分支要编。代价是拿不到 `IsError`，理由写在注释里了。

参数那一侧要解决的是"同一次调用，两次写出来不一样"：

```go
// canonicalArguments 把参数规范化成可比的字符串。模型对同一次调用两次可能
// 给出键序不同的 JSON（对象成员顺序在协议上没有意义），直接比原始字节会把
// 一次调用看成两次。解析再编回去就归了序——encoding/json 编 map 时按键排序，
// 这是标准库保证的行为。
//
// 解析用 UseNumber：默认解析进 float64 会把 1 与 1.0 归一（宽容），但同时
// 让超过 2^53 的整数掉精度，两个不同的数会被算成同一个。宽容的方向是误报，
// 误报会掐掉一次健康运行，比漏报更难查。UseNumber 保留字面量，代价是 1 与
// 1.0 算两个签名：那是漏报方向（同一件事因为数字写法不同被当成两轮），但模型
// 不会无缘无故在两轮之间改数字写法，撞上的概率远低于精度塌陷。
//
// 解析不了、或后面还跟着别的 token（模型给过截断或拼接错位的 JSON）时原样
// 返回：它是模型给的字符串，比不出错，也不该在这里报错。
func canonicalArguments(arguments json.RawMessage) string {
	decoder := json.NewDecoder(bytes.NewReader(arguments))
	decoder.UseNumber()
	var decoded any
	if err := decoder.Decode(&decoded); err != nil {
		return string(arguments)
	}
	// 合法的 JSON 值之后除了 EOF 不该再有内容。有的话说明这串东西不是我们
	// 以为的一整个值，别编回去——编回去会丢掉后面那段，让两个不同的参数
	// 撞成同一个签名。
	if _, err := decoder.Token(); !errors.Is(err, io.EOF) {
		return string(arguments)
	}
	encoded, err := json.Marshal(decoded)
	if err != nil {
		return string(arguments)
	}

	return string(encoded)
}
```

一句话概括这一节的取向：宽容的方向是误报（把健康运行杀掉），严格的方向是漏报（这一次没抓住），两条路都有人走，这里一律选严格。

### 4. 连续计数与阈值

判据只有一条，所以状态只需要两个字段：

```go
// loopGuard 是内建的循环检测：连续若干轮的工具调用与结果都完全相同就判循环。
// 判据只有一条，因为它只需要一个状态：上一轮的签名和它连续出现了几次。
// "连续"而不是"窗口内累计"——连续是更强的信号、误报更低，也让状态退化
// 成两个字段；代价是抓不到 A→B→A→B 这种振荡，真出现了再加窗口。
type loopGuard struct {
	limit   int
	last    string
	repeats int
}

// …（中间是 newLoopGuard，下一段单说）

// observe 记下这一轮的签名并判定。返回 nil 表示这一轮不是循环。
// limit <= 0 表示关闭，直接返回——装配不做条件判断，关不关由值决定。
func (g *loopGuard) observe(report TurnReport) error {
	if g.limit <= 0 {
		return nil
	}

	signature := signatureOf(report.Message.ToolCalls, report.ToolResults)
	if signature == g.last {
		g.repeats++
	} else {
		g.last, g.repeats = signature, 1
	}
	if g.repeats < g.limit {
		return nil
	}

	return pierrors.ErrRunLoopDetected.Wrap(fmt.Errorf(
		"连续 %d 轮的工具调用与结果完全相同：%s", g.repeats, clipSignature(signature)))
}
```

阈值 `defaultLoopGuardTurns` 取 3。取 3 的理由是正常流程里的样子：读同一个文件两遍很常见——先看结构、改完再确认。两遍就判会误杀这种，三遍还一模一样，基本可以确定这一轮没产生任何变化。

旋钮的形状与 `MaxTurns` 不一样：

```go
	// MaxTurns 是单次运行的模型调用次数上限，<= 0 时取默认值。
	MaxTurns int
	// LoopGuardTurns 是循环检测的阈值：连续这么多轮的工具调用与结果完全相同
	// 就终止运行。0 取默认值（3），负数关闭——关闭之后只剩 MaxTurns 兜底。
	LoopGuardTurns int
```

差别在于关闭需要一个表达方式——用 0 表示关闭，就得给"取默认值"另找一个哨兵值，负数比哨兵值清楚。

关闭那条路落在 `observe` 里，不在装配处：

```go
// newLoopGuard 按装配处的配置造一个判据：0 取默认值，负数表示关闭。关闭不是
// 不装钩子，而是把 limit 设成非正数，由 observe 自己吞掉——装配处因此不用写
// 条件分支，Loop.run 里那个 l.afterTurn != nil 也就永远是真。
func newLoopGuard(configured int) *loopGuard {
	if configured == 0 {
		return &loopGuard{limit: defaultLoopGuardTurns}
	}

	return &loopGuard{limit: configured}
}
```

签名会进错误文案，所以要能截断：

```go
// clipSignature 把签名截到能进错误文案的长度，按 UTF-8 边界切，免得日志里
// 出现半个汉字。只用在错误文案上，不参与比较——比较用的是完整签名。
// 签名通常由 json.Marshal 产出、是合法 UTF-8；走 canonicalArguments 原样
// 返回那条路时可能不是（那是模型给的原始字节）。所以这里按 ValidString 往前
// 退到边界，最坏退到 0、文案只剩一个省略号——可接受的降级，不为它加分支。
func clipSignature(signature string) string {
	const max = 240
	if len(signature) <= max {
		return signature
	}
	cut := max
	for cut > 0 && !utf8.ValidString(signature[:cut]) {
		cut--
	}

	return signature[:cut] + "…"
}
```

截断只用在文案上，比较用完整签名：截了再比，两条只在第 241 个字节之后不同的签名会撞成同一条，那是白送的误报。

### 5. 装配：agent.go 里的几行

`Agent` 上多了一个字段，它同时也是"计数什么时候清"的说明：

```go
	// guard 是本轮的循环检测。判据在装配时建一次、挂在循环的轮末钩子上，
	// 计数每轮 Run 开头清空——它记的是"连续几轮"，跨 Run 留着就会把两次
	// 不相干的运行接成一段。
	guard *loopGuard
```

装配是几行：

```go
	agent := &Agent{
		contextBuilder: NewContextBuilder(opts.WorkDir),
		session:        opts.Session,
		provider:       provider,
		window:         session.NewWindow(int64(opts.ProviderOptions.ContextWindow)),
		onCompaction:   opts.CompactionObserver,
		guard:          newLoopGuard(opts.LoopGuardTurns),
		runtime:        runtime,
	}
	// …
	agent.loop = NewLoop(
		provider,
		WithScheduler(tools.NewScheduler(registry, maxParallel, opts.Observer, middleware.Defaults()...)),
		WithTextObserver(opts.TextObserver),
		WithMaxTurns(opts.MaxTurns),
		WithBeforeTurn(agent.compactBeforeTurn),
		WithAfterTurn(func(_ context.Context, report TurnReport) error {
			return agent.guard.observe(report)
		}),
		WithMessageObserver(agent.recordMessage),
	)
```

注意装配处没有 `if opts.LoopGuardTurns > 0` 这种条件：装不装由 `limit` 的值决定，关掉之后 `observe` 立刻返回。少一个"装不装钩子"的判断，`Loop.run` 里那个 `l.afterTurn != nil` 也就永远是真。

最后一个容易漏的地方在 `Run` 的开头：

```go
	// 一个 Run 一轮账：上一轮的写入错误、本轮起点与循环检测的计数都不带进
	// 这一轮。检测计数必须在这里清——它记的是"连续几轮"，跨 Run 留着就会把
	// 两次不相干的运行接成一段。
	a.writeErr = nil
	a.headLeafID = ""
	a.guard.reset()
```

不清这个计数，同一个 Agent 上两次毫不相干的运行，各自只调了一次同一个工具，就会被算成"连续两轮"。

### 6. 两个码怎么分

```go
	// ErrRunLimitExceeded 运行控制
	ErrRunLimitExceeded = New(50000, "运行预算超限")
	ErrRunLoopDetected  = New(50001, "检测到循环调用")
```

调用方按 `CodeOf` 分流就行：

1、50000 是"这次活的量超了"。可以调大上限重试，或者换个做法把活拆开。
2、50001 是"这个做法本身卡住了"。重试没用，重试还是那三轮，得换做法。

两条刹车不互相压制：检测器先命中就报 50001，`maxTurns` 先命中就报 50000，各报各的。

它们也不合并。合并的两种写法都不成立：用检测取代 `maxTurns`，抓不到"每轮都不同但都没进展"，也抓不到"一直在推进但战线过长"；把 `maxTurns` 收进检测器，等于把兜底从必然存在的地方挪到一个可能不存在的地方——`defaultMaxTurns` 在 `NewLoop` 里就生效，任何自己拼 `Loop` 的人都有；检测器是 `Agent` 装配时才挂上去的，`Agent` 之外的用法拿不到。

## 与 pi.dev 差在哪

参考实现仍然是 pi.dev（[badlogic/pi-mono](https://github.com/badlogic/pi-mono)），下面这些都在提交 `c7cdb46` 上核过：

| 事实 | 位置 |
|---|---|
| 主循环 `while (true)`，没有迭代上限 | `packages/agent/src/agent-loop.ts:177` |
| 退出条件：stopReason 是 error / aborted | `agent-loop.ts:221` |
| 退出条件：这一轮没有工具调用 | `agent-loop.ts:231` |
| 退出条件：宿主钩子 `shouldStopAfterTurn` 返回 true | `agent-loop.ts:258` |
| 退出条件：没有 follow-up 消息 | `agent-loop.ts:268` |
| 钩子的类型与文档（返回 boolean） | `packages/agent/src/types.ts:230`、`agent/src/agent.ts:123` |
| 钩子拿到的现场 | `packages/agent/src/types.ts:130-140` |
| 工具批内声明终止（整批才生效） | `packages/agent/src/agent-loop.ts:645` |
| 全仓没有轮次上限，也没有循环检测 | `grep -rniE "maxTurns|maxIterations|maxSteps"` 只命中 `packages/ai/test/stream.test.ts:284` 一处，是测试里防死循环的局部变量；`loop.?detect|oscillat` 零命中 |

它把"什么叫跑偏"整个判给宿主，自己只留机制。这边的机制是照搬的（轮次结束钩子），有两处明确偏离：

一是钩子返回 `error` 而不是 `bool`。pi.dev 那边返回 `true` 时循环正常结束、不发任何信号（`agent-loop.ts:259-260`）；这边的 `Run` 已经是错误码分流的形状，白丢一个能带码的信息不划算。

二是缺省装一个判据。pi.dev 缺省什么都不装——它的主场景是交互式 TUI，卡住了有人按 Esc；`cmd/harness` 是无人值守的驱动器，缺省没有防护等于没有。

还有一处位置上的差别：pi.dev 的 `shouldStopAfterTurn` 在 `if (toolCalls.length > 0)` 那个块之外（`agent-loop.ts:258`），模型每一轮都会被问一次，包括不带工具调用的那一轮。这边只在带工具调用的轮问——钩子要回答的是"还要不要开下一轮"，模型不要工具时这个问题不存在，上面已经 `return` 了。

## 总结

回到流程图，这一层是两个判定点加一个判据。

轮首那条（`pi/loop.go:207`）管总量，默认 100 次模型调用，退出形状是带消息返回，好让调用方看到跑到了哪一步。它住在 `Loop` 里，所以谁自己拼循环都白得一个兜底。

轮末那条（`pi/loop.go:270`）管形态，钩子拿到一轮的现场（助手消息 + 结果），返回错误就是收尾。它只管机制：什么时候问、问到什么、怎么收尾；判什么由消费者决定。

判据（`pi/loopguard.go`）只有一条：整轮的工具调用与结果连续三轮一模一样。签名里顺序照抄、结果也带上、参数规范化，每一处取舍的方向都是减少误报——误报会掐掉一次健康的运行，比漏报难查得多。

压缩那篇结尾说下一篇写上下文组装，这篇插在了它前面：压缩对付的是"聊不下"，这篇对付的是"停不下"，两件事都落在运行的边界上。下一篇照原计划写上下文组装，也就是 `ContextBuilder` 那一块——系统提示词、技能目录、AGENTS.md 和历史怎么排成一次请求。感兴趣的话关注一下，防止走丢。
