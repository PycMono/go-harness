# 从零手搓 Harness（开篇）：两年换了三个框架，我决定自己撸一个

All in AI 也有 2 年了吧，框架换了好几个，从 Coze 到 LangChain，再到现在比较火热的 Loop Agent，每个框架多多少少都能总结出一些经验。Harness 我虽然没有把自己的框架真正搭建上线，基于公司的 Harness 产品，也训练出了几个 AI 产品（已交付客户）。

所以就有了这个专栏——我会参考现在比较流行的几个实现，比如 [pi.dev](https://pi.dev)，也会看看 DeepSeek Harness、Claude Code、OpenClaw 这些项目。只求把核心思想搞懂，不是完完全全复制，没那个时间也没那个精力。

整个系列用 Go 写，不是 JS 也不是 Python。

这是开篇，不写代码。完整代码放在 GitHub：[https://github.com/PycMono/go-harness](https://github.com/PycMono/go-harness)，后面每篇涉及到的文件都可以直接翻仓库对照。

## Coze：可视化的天花板

先说说 Coze 吧，应该是国内发布比较早的 AI 平台了，简单、上手也容易，脚本语言是 js 和 py，平台提供可视化的工作流搭建，插件和技能也比较丰富。早期需求都是基于这个平台开发的。

但是 Coze 有一些非常明显的缺点。做过 SaaS 的应该都知道国内客户环境是什么样的，客户既要又要，总结下来就是啥都想要。

落到实际项目上：

1、要么无法实现客户需求，流程是死的，为了实现客户需求只能不停打补丁；
2、要么就是实现非常复杂，为了兜住各种边界情况，整个流程看板的节点连线复杂到爆炸，过两个月自己都看不懂；
3、无法熔断、重试、异常回退这些处理，请求报错就报错了，流程中保存的数据还会导致你无法重试——重跑一次，节点里的脏数据还在，结果反而是错的，很大程度牺牲了正确性。

基于 Coze 我也开发了好几个版本，上线后效果并不咋样。Coze / 扣子还是适合搭工作流和机器人，适合那种路径确定的场景。执行路径需要模型临场决定的活，靠画图是走不通的。

## Loop Agent：把流程编排交还给模型

LangChain 我用得不算深，简单带过。它的抽象层叠得太多，链式调用一层包一层，出了问题很难说清楚到底哪一层做了什么，调试成本很高。想彻底理解 Agent 是怎么跑起来的，它更像一个黑盒，这也是我当时跳走的原因。

现在比较火热的是 Loop Agent。它不再是流程化的"这步干啥，下一步干啥"，不需要你用代码去规定"先执行 A，再执行 B"。你只需要给它一个包含当前状态的上下文（Context），并告诉它"这里有几个工具"，它就能自主推导出下一步该干什么。一轮结束，把结果塞回上下文，再来一轮，直到模型认为任务完成。

基于 Loop Agent 框架开发的客户智能体，明显能感觉到智商在线了，不是之前那个傻傻的 250。

原因不复杂。排障、调研、多轮操作这类任务，执行路径本来就取决于上一步的返回结果，这种活不该被人提前画死，只有模型在每一轮看到最新状态后才有资格做决策。另外工具执行超时了、返回异常了？把报错塞回上下文，模型下一轮自然就会换个思路。传统编排里的"异常 → 崩溃"，在这里变成了"异常 → 重新规划"，这恰恰是 DAG 最缺的弹性。

当然 Loop Agent 也不是银弹，循环轮数不可控、Token 消耗失控、模型一条道走到黑出不来，这些问题就是框架层要解决的了。

## Harness：套在大模型外面的"微型操作系统"

我把 Harness 理解为套在大模型外面的那个"微型操作系统"：ReAct 循环、工具执行、上下文管理都在里面跑。模型只负责"想"，Harness 负责"跑"。

## 航海图

整个系列就围着这张图展开，每一篇点亮其中一块：

```text
用户输入
   │
   ▼
cmd/                        程序入口：读配置 → 装配 → 起 Run
   │
   ▼
Agent Core（pi 包根）        Loop 主循环 · Run 契约 · Runner 装配
   │                        每轮 = 问模型 → 执行工具 → 结果回填
   │ 依赖
   ▼
能力域（每个目录一个能力）
   ├─ pi/ai + providers     模型适配：OpenAI / Anthropic 双协议
   ├─ pi/tools              工具执行：Registry + bash/read/write/edit
   ├─ pi/context            上下文：Prompt 组装、剪枝、压缩
   ├─ pi/governor + pi/error  治理：预算准入、资源上限、统一错误码
   ├─ pi/loopdetect         工具行为循环检测
   ├─ pi/skills             SKILL.md 渐进式加载
   ├─ pi/middleware         工具执行链中间件
   ├─ pi/sandbox            本地命令执行边界
   ├─ pi/mcp                外部工具生态
   └─ pi/observability      追踪与用量计量
   │
   ▼
pi/ai（底座）                内部方言：Message / ToolCall / Usage / StreamEvent
                            不依赖任何其他包
```

依赖方向单向：底座不认识能力域，能力域不认识 Agent Core，最上层负责装配。后面每篇文章开头我会把这张图贴出来，标出本篇讲的是哪一块。

## 路线图

整个系列计划这么走，每篇对应仓库里一个目录：

| # | 主题 | 对应目录 | 一句话说明 |
|---|---|---|---|
| 1 | 模型封装 | `pi/ai` | 统一消息模型 + OpenAI / Anthropic 双协议适配 |
| 2 | 工具与 Loop 循环 | `pi/tools` | 工具注册、执行域，驱使模型连续干活的主循环 |
| 3 | 错误与治理 | `pi/error`、`pi/governor` | 统一错误码、资源上限、预算准入、终止分类 |
| 4 | 上下文与会话 | `pi/context` | System Prompt 组装、会话管理、剪枝 |
| 5 | 压缩与记忆 | `pi/context` | 上下文超限后的压缩策略与跨会话记忆 |
| 6 | Skills 渐进式加载 | `pi/skills` | SKILL.md 的发现、解析与按需注入 |
| 7 | 可观测性 | `pi/observability` | 追踪、用量计量，跑起来之后才知道钱花哪了 |
| 8 | 重试恢复 | `pi/governor` | 哪些错误值得重试、Run 断点怎么续 |
| 9 | 沙箱 | `pi/sandbox` | 本地命令执行的边界与隔离后端 |
| 10 | MCP | `pi/mcp` | 经 MCP 协议接入外部工具生态 |
| 11 | 中间件 | `pi/middleware` | 工具执行链上挂中间件：审计、限流、脱敏 |

## 结尾

开篇就到这。下一篇进入代码，从最底层的模型封装开刀——《从零手搓 Harness 之 Provider 抽象，吃下 OpenAI / Anthropic 双协议》，讲怎么用一个 `Provider` 接口把两大协议的差异全部关在适配层里。

感兴趣的话关注一下，防止走丢。